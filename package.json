{
  "name": "tasks-dispatcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm exec vitest run --config vitest.workspace.ts packages/core/tests && pnpm exec vitest run --config vitest.workspace.ts packages/workspace-runtime/tests && pnpm exec vitest run --config vitest.workspace.ts apps/cli/tests && pnpm exec vitest run --config vitest.workspace.ts apps/desktop/src/main/__tests__",
    "test:watch": "vitest --config vitest.workspace.ts",
    "dev:cli": "pnpm --filter @tasks-dispatcher/cli dev",
    "dev:desktop": "pnpm --filter @tasks-dispatcher/desktop dev"
  },
  "devDependencies": {
    "@types/node": "25.5.0",
    "tsx": "4.21.0",
    "typescript": "6.0.2",
    "vitest": "4.1.2"
  }
}
