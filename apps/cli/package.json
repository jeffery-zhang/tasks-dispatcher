{
  "name": "@tasks-dispatcher/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@tasks-dispatcher/workspace-runtime": "workspace:*"
  },
  "devDependencies": {
    "tsx": "4.21.0"
  }
}

